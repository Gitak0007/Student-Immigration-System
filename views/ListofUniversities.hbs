<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
	 <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel='stylesheet' href='/stylesheets/style.css' />
 <style>

    /* Set height of the grid so .sidenav can be 100% (adjust as needed) */
  form {
    display: block;
    margin-top: 0em;
}
strong {
    color: #0060AF;
    font-size: 18px;
}
@media (min-width: 576px)
.col-sm-2 {
    -webkit-box-flex: 0;
    -ms-flex: 0 0 16.666667%;
    flex: 0 0 16.666667%;
    max-width: 16.666667%;
}
.form-control {
    display: block;
    width: 100%;
    padding: .375rem .75rem;
    font-size: 1rem;
    line-height: 1.5;
    color: #495057;
    background-color: #fff;
    background-clip: padding-box;
    border: 1px solid #ced4da;
    border-radius: .25rem;
    transition: border-color .15s ease-in-out,box-shadow .15s ease-in-out;
}
.col-form-label {
    padding-top: calc(.375rem + 1px);
    padding-bottom: calc(.375rem + 1px);
    margin-bottom: 0;
    font-size: inherit;
    line-height: 1.5;
}
    div.content ul li div h3 a {
    font-size: 18px;
    font-weight: normal;
    color: #4a4a4a;
    text-decoration: none;
}   
a:link {
    -webkit-tap-highlight-color: #333;
}
a {
    color: #0060AF;
    word-break: break-all;
    display: inline-block;
    text-decoration: underline;
}

div.content ul li div table td {
    padding-right: 40px;
    padding-bottom: 4px;
    
}

h1 {
    font-size: 24px;
    color: #4A4A4A;
    font-weight: normal;
}
h3 {
    font-size: 17px;
    color: #4A4A4A;
    font-weight: normal;
}
    }
  </style>
  </head>
   <body>
	<h1>{{title}}</h1>
	<div class="content">
	<ul id="searchResults">                    
            <li>
            <div>
                {{#each results}}
                

            <h3>
                    <i class="hidden-xs"></i>
                    <strong>{{this.ProgramName}}</strong>
                  
                    <span>{{this.universityName}}</span>
                </a>
            </h3>
            <table><tbody>
                <tr><td>Costs</td><td>{{this.tutionFee}}</td></tr>
                <tr><td>Course Language </td><td>{{this.courseLanguage}}</td></tr>
                <tr><td>Date</td><td>{{this.admissionSemester}}</td></tr>
                <tr><td>Course Website </td><td>
                <a href={{this.courseWebsite}}>{{this.courseWebsite}}</a></td></tr>
            </tbody></table> 
           
            {{/each}}
            </div>
            </li> 
	</ul>
</div>	
  </body>
</html>
